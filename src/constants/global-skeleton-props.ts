import {SkeletonProps} from 'react-loading-skeleton';

const globalSkeletonProps: SkeletonProps = {
    baseColor:      'var(--loading-base)',
    highlightColor: 'var(--loading-highlight)'
};

export default globalSkeletonProps;